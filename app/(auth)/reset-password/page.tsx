import { ResetPasswordForm } from "./_components/reset-password-form";

export const metadata = { title: "Reset password — Lecturra" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
